import { Button, Card } from "@complex/ui-lib";
import { formatDate } from "@complex/utils";

console.log(`Rendering frontend at ${formatDate(new Date())}`);
console.log(Button());
console.log(Card());
